;CMP = compare
;JG  = jump if greater than
;JL  = jump if less
;JGE = jump if greater than or equal
;JNG = jump if not greater than
;JE  = jump if equal
;JNE = jump if not equal
;JMP = jump
;INC = increase
;DEC = decrease


;db = 8 bit
;dw = 16 bit
;ds = data segment
;@data = to find data from registar
;lea = for print  string  Load effective assress

;xchg bh,ch  for exchange number
;mov ah,4ch for exit DOS mode
;small ----> code
       |          In one segment
       |---> data
       
;Medium ---> code More then one segment
       |          
       |---> data  In one segment
       
;Compact ---> code  1
       |          
       |---> data  More then one segment
       
;Large ----> code  More then one segment
       |          
       |---> data  More then one segment  array size<=64kyb
       
;Huge ----> code  More then one segment  
       |         
       |---> data  More then one segment  array size<=64kyb  
       
;mov bh,35h
  neg bh                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                
